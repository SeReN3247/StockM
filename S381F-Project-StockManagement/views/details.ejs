<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
    <style>
        p{
            font-size: 1.5em;
        }
    </style>
</head>
<body>
    <div class="board text-center">
        <h1>Item Information</h1>
    </div>
    <div class="row">
        <p>Item ID:  <% if (typeof (item.itemID) !== "undefined") { %><%= item.itemID %><% } %></p><br>
        <p>Item Name:  <% if (typeof (item.name) !== "undefined") { %><%= item.name %><% } %></p><br>
        <p>Manufacturer:  <% if (typeof (item.manufacturer) !== "undefined") { %><%= item.manufacturer %><% } %></p><br>
        <p>Manufacturer Contact:  <% if (typeof (item.phone) !== "undefined") { %><%= item.phone %><% } %></p><br>
        <p>Description:  <% if (typeof (item.description) !== "undefined") { %><%= item.description %><% } %></p><br>
    </div>
        <a href="/edit?_id=<%= item._id %>" >Update the information</a><br><br>
        <a href="/delete?_id=<%= item._id %>&owner=<%= item.ownerID %>">Delete the information</a><br><br>
        <a href="/home">Home</a>
</body>
</html>

